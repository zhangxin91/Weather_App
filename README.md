# Weather_App
This smart weather API allows you to retrieve real-time weather data using a city name or GPS coordinates. It validates requests, queries a reliable external source, processes the data (temperature, humidity, forecasts, rain), and returns a structured JSON response.
